name: 'lerna-changed'
description: 'Github action for determining the changed packages in a lerna monorepo'
author: 'Allen Evans'
inputs:
  cwd:
    description: 'Current working directory'
    default: ''
  variable:
    description: 'Set the environment variable to set with the changed package string'
    default: ''
runs:
  using: 'node12'
  main: 'lib/main.js'
